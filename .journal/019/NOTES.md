---
id: 019
title: Design agentcompute, disposable compute for agents
started: 2026-09-11
---

## 2026-09-11 07:25 — Kickoff
Goal for the session: not yet stated; the user asked only to open a new session.
Current state of the world: meta repo `master` at `b62bc98` (5 behind origin). Four-node IncusOS cluster live (nas01 + lab01–03); storage converged. T48 (lab VLAN 30 datapath) remains blocked on the IncusOS stable release carrying lxc/incus-os#1306; nodes run `202608201218` with `auto_reboot: false`. Sessions 011, 012, 014–018 are still `in-progress` in `INDEX.md`. Latest closed session with a summary is 013 (2026-08-23).
Plan: wait for the user's actual request, then update the title and this log.

## 2026-09-11 07:54 — Goal stated; vocabulary draft written
Goal: initial design + planning docs for `agentcompute`, a lab-specific CodeMode MCP server giving agents disposable containers/VMs (Linux/Windows/macOS), desktops, and arbitrary network topologies. Josh: start with the agent vocabulary.
Inputs read: `~/code/meigma/codemode` (public-api, mcp-tools refs) and `~/code/meigma/template-mcp-codemode` README. Load-bearing CodeMode constraints: keyword-only flat scalar inputs (no lists/objects), 1 MiB value cap, default 5 s / 100 native calls per program, dotted names ≥2 segments.
Facts checked: Incus VGA console is SPICE-only (no Go client) → desktop I/O via in-guest VNC. VLAN 40 exists only on `gw01 eth2` (address plan) → sandbox VLAN to the cluster nodes is a real prerequisite (networking + fleet; first "instance VLAN" consumer).
Wrote `docs/docs/designs/drafts/agentcompute.md` on worktree `feat/agentcompute-design` (`e636c5d`), nav entry added, strict build passes. Vocabulary roots: `sandbox`, `image`, `instance`, `net`, `desktop`. Key provisional choices: sandbox = Incus project pinned to one member; management NIC on the sandbox VLAN by default; screenshots returned as HTTP URLs; blocking ops with raised CodeMode budgets; `router` image + `instance.exec` instead of routing vocabulary; macOS via Tart on an Apple Silicon host.
Open for Josh: the word `sandbox` (collides with `sandbox01`/`GilmanLab/sandbox`); which Mac hosts Tart; VLAN 40 vs new VLAN; reaching guests that opt out of the management NIC.
Note: session 018's only commit is "assess disposable desktop testing" (`.journal/018/NOTES.md`) — likely prior art; not read (protocol: other sessions' NOTES stay closed unless asked).

## 2026-09-11 08:03 — Lume and Cua Driver adopted into the draft
Josh asked: lume over tart? Cua for computer use? Researched cua.ai docs (Lume 0.5.3 CLI ref, Cua Driver platform support + integration model + MCP tool catalog, Sandbox SDK runtime support 0.4.3) and trycua/cua issues.
Facts: Lume = MIT, `lume serve` HTTP API :7777, VNC built in, OCI images, NAT-only networking (cua#1007 open, NAT bug cua#483). Tart = Fair Source, CLI only, bridged/softnet, Packer plugin. Cua Driver = Rust in-guest runtime, 56 MCP tools (AX tree + element tokens, pixel fallback, screenshots, CDP browser), every tool also one-shot CLI `cua-driver <tool> '<json>'` with `--screenshot-out-file`; transports are stdio / local socket only. Cua Sandbox SDK = Python, runtimes QEMU/Docker/Hyper-V/Lume/Android, no Incus, snapshots not implemented.
Decisions (provisional, in draft `7c4bc0d`): Lume replaces Tart; Cua Driver over `instance.exec` replaces raw VNC as the primary desktop path (VNC = fallback). `desktop.*` collapses to `info`, `enable`, `call(tool, args_json)`, `screenshot`. Big win: desktop work needs no guest network reachability, so former Open Question 2 (guests with `management=False`) is dissolved. Rejected: Cua Sandbox/Fleets as substrate (listed as buy alternative). New open questions: Driver-in-image recipe details/TCC on macOS; `desktop.call` JSON-string ergonomics.
Observation: the omp harness `computer` prelude (ax refs, background delivery, BackgroundUnavailable) is cua-driver-shaped, so agents will find the guest desktop model familiar.

## 2026-09-11 08:27 — OVN adopted as the sandbox fabric
Josh pointed at the IncusOS OVN service reference. Verified (docs page + `incus-osd/api/service_ovn.go`): the service is chassis-only (`enabled`, `database` = external NB/SB address, TLS client material, `tunnel_address/protocol`, `ic_chassis`). No OVN central on IncusOS; still requires an external `ovn-northd` + NB/SB. CVE-2026-40243 (Incus OVN TLS trust flaw) noted for the TLS step.
Ruling in draft (`11916c9`): OVN becomes the target sandbox network fabric. Gains: cross-member sandboxes (Geneve over VLAN 10 until T48 clears), native logical router/SNAT/DHCP/DNS, network ACLs (baseline deny to mgmt/OOB at the virtual router), forwards (agent reachability without a management NIC), peering. `router` image stays for routing protocols, exotic NAT, netem, VPNs. Vocabulary: `net.create(kind=ovn|bridge, cidr, dhcp, nat, dns)`, new `net.peer`, `net.acl.add/remove`, `net.forward`; `sandbox.create` drops `host`, `instance.create` gains `network?` and `host?`; "management NIC" replaced by a per-sandbox `default` NAT'd OVN network. Prerequisites now: OVN central (spike on sandbox01; target OpenTofu VM on nas01 on the VLAN 10 bridge), chassis config via fleet `cluster/` pyinfra, sandbox VLAN as the OVN uplink `physical` network with `ipv4.ovn.ranges`. Plan step 3 = one-day OVN spike with an explicit fallback to per-member bridges. 
Tracker follow-up for close: T11 ("bridged VLANs now; OVN needs external DB") gets agentcompute as the first OVN consumer; T10/T11 note that OVN central may later serve Talos networks.

## 2026-09-11 09:29 — Go architecture synthesized
Ran `software-architect` (GoArchitect) then `complexity-reviewer` (GoComplexity) on its output. Middle-ground result written to `.journal/019/ARCHITECTURE_GO.md`.
Kept from the architect: template structure unchanged, explicit per-capability registration, Incus-as-registry with `user.agentcompute.*` metadata, re-scanning reaper, keyed gate, draining capped exec writers, transient disk screenshot store, 15m/1000-call CodeMode limits, real-Incus integration lane. Accepted from the reviewer: no 24-method cross-backend seam yet, cut Generation/deleting-marker/owner-resolution/second semaphore/durable screenshot index/configurable knobs, gate narrowed to control-plane mutations, two error sentinels only, defer Lume adapter + sidecar. Rejected from the reviewer: "call Incus directly from handlers" — handlers see `compute` types only so the Lume seam is extractable later; kept subject as a write-only metadata key. Resolved the reviewer's seven slice-1 blockers as decisions (name rules, default bridge shape, metadata keys, config schema, create mapping, DTO conventions).
Finding verified myself (`codemode/errors.go`, `dispatch.go`, `mcp-tools.md`): handler errors reach the agent only as the fixed text `capability failed`; Starlark cannot catch, so programs abort with no cause. Needs an upstream CodeMode change (`AgentError`-style sanitized message) or a softened draft promise — flagged as the #1 decision for Josh. Also: Driver CLI spelling is `cua-driver call <tool> <json> --screenshot-out-file`; draft to be corrected.

## 2026-09-11 09:35 — Filed meigma/codemode#58
[meigma/codemode#58](https://github.com/meigma/codemode/issues/58): "Let handlers opt in to an agent-visible failure message" — proposes `codemode.AgentError{Message}`; when present in the handler error chain, the native-call failure becomes `capability failed: <sanitized message>` (256-byte cap, single line), reusing the #24 `WithSafeDetail` path (`internal/execution/detail.go`; hook at `dispatch.go:182`; both projection functions; worker frame must carry the suffix). Default non-disclosure unchanged. agentcompute's "actionable errors" promise depends on it; until then slice 1 ships coarse errors.

## 2026-09-11 10:27 — codemode#59 merged; docs aligned
Josh shipped [meigma/codemode#59](https://github.com/meigma/codemode/pull/59) (`4b498a2`, master, post-0.2.0): `codemode.AgentError{Message}` exactly as proposed — `errors.As` opt-in, only `Message` attached, non-printables → spaces, invalid UTF-8 repaired, ≤256 bytes with `...`, MCP text `capability failed: <detail>`, program still aborts, ordinary errors stay hidden, panic/policy/conversion cannot opt in. Read landed `public-api.md` section.
Updated `ARCHITECTURE_GO.md`: error contract now a rule (compute service returns AgentError for agent-actionable failures; backend faults stay bare + logged; `ErrNotFound` mapped to AgentError at the handler layer); risk = pin a CodeMode release containing #59. Updated draft (`acf1874`): Driver CLI spelling `cua-driver call <tool> <json> --screenshot-out-file`, name grammar + reserved names in conventions, error convention cites #59, prerequisite 5 = CodeMode release with AgentError.

## 2026-09-11 10:52 — Image pipeline research
Josh asked for a GitOps-style image build/publish pipeline and whether `~/code/meigma/incus-gh-runner` should host CI on the lab. Ran three researchers (LinuxImages, WindowsImages, MacImages); synthesis in `.journal/019/IMAGE_PIPELINE.md`.
Facts: cluster is Incus 7.4 (controller needs ≥7.0 ✓); extra remotes exist: `ovh-incusos` (off-lab IncusOS box) and `sandbox01` (HTTPS over Tailscale). `incus-gh-runner` connects via Unix socket only (`internal/adapters/incus/client.go`) → cannot target the cluster over HTTPS without a feature; its baseline sets `security.nesting=false` on runner VMs → no nested KVM in runners. imgoci has `incus-vm` representation but no container one; `simplestreams-oci` is still a bare template; Incus does not read imgoci.
Shape recommended: runners assemble (distrobuilder, root, no KVM); the lab bakes (Windows unattended install, boot-tests, macOS seed via Lume) in an `image-build` project driven over the Incus API; CI orchestrates; GHCR stores imgoci releases; a reconciler (agentcompute catalog `ref@digest` now; `simplestreams-oci` later) imports. Windows/macOS redistribution licensing is release-blocking (private GHCR at most); macOS TCC consent is one manual step per seed (semi-GitOps). Cua Driver 0.28.0 is a prerelease — pin a reviewed version.
Candidate upstream work (not filed): incus-gh-runner remote-HTTPS mode; imgoci `incus-container`; simplestreams-oci as native last mile; Lume pull-by-digest.

## 2026-09-11 10:57 — Filed meigma/incus-gh-runner#65
[meigma/incus-gh-runner#65](https://github.com/meigma/incus-gh-runner/issues/65): "Connect to Incus over HTTPS with a client certificate". Seams named: `ConnectUnix` (`internal/adapters/incus/client.go`), single call site `internal/runtime/runtime.go:263`, `config.Incus`, the `validate --socket` subcommand. Proposal: `incus.url` + `client_cert_file`/`client_key_file` (systemd `LoadCredential` drop-in) + required pinned `server_cert_file`, mutually exclusive with `socket`; `ConnectHTTPS` via `incusclient.ConnectIncusWithContext`; posture improves via project-restricted trust certs vs today's root-equivalent `incus-admin`. Cluster placement (`incus.target`) and cluster-aware baseline validation flagged as follow-ups. Motivation: IncusOS has no host shell, so the controller can't run on the cluster at all today.

## 2026-09-11 12:24 — Both upstream fixes released
codemode [v0.2.1](https://github.com/meigma/codemode/releases/tag/v0.2.1) (2026-09-11) contains #59 `AgentError`. incus-gh-runner [v2.0.0](https://github.com/meigma/incus-gh-runner/releases/tag/v2.0.0) (2026-09-11, PR #66 closing #65) ships pinned HTTPS mode: `incus.url` + `client_cert_file` + `client_key_file` + `server_cert_file`, mutually exclusive with `incus.socket`; breaking: local mode must now set `incus.socket` explicitly. Verified via the v2.0.0 `configuration.md` and the release body.
Updated: `ARCHITECTURE_GO.md` (pin codemode ≥ v0.2.1), draft prerequisite 5 (`ce168c6`), `IMAGE_PIPELINE.md` (controller can run as a cluster VM with a project-restricted cert; `sandbox01` demoted to spike host; remaining gap = cluster-aware `deploy/incus/` baseline; suggested-order step 3 updated).

## 2026-09-11 12:41 — Implementation plan; TECH_NOTES found stale
Ran `planner` (AgentcomputePlan) over the draft + ARCHITECTURE_GO + IMAGE_PIPELINE + this log; result saved to `.journal/019/PLAN.md` (3.1k words, 9 phases, spike-first; Phase 1 = publish ONE `router` image end to end, Phase 2 = Go slice 1, Phase 3 = OVN spike in parallel; sandbox-name blocks before durable registrations, VLAN choice blocks at Phase 5 durable uplink, Mac choice blocks at Phase 8).
**Correction to TECH_NOTES/VISION (promote at close):** sessions 016/017 resolved T48 and never updated the shared notes. Verified in git: fleet `2ef3177` (2026-08-25) "T48 blocker is gone: IncusOS 202608242359 ships the ice DDP package"; lab bonds are 802.3ad LACP (`networking#16` restored LAGs on sw-core01); fleet `d0e0b5e`+`ff9d4b0` admit VLAN 40 on the fast links and declare `fast40` host-side — instances attach with `parent=fast40` (Incus-created `vlan=` sub-interfaces on `fast` pass nothing), DHCP from gw01, verified cross-node; `networking#17` tags VLAN 40 on LAGs + nas01 link; `#18/#19` sandbox↔cluster access + tailnet→VLAN 40 masquerade; root `#24–#28` documented it (address-plan now describes `fast40`) plus ADR-0005. Nodes re-pinned to `202608242359` (fleet `3da00ad`). T48 → resolved; T11 note "instance VLANs on fast links" → done for VLAN 40.
Consequences applied: draft `cce8670` — VLAN 40 context bullet, OVN `tunnel_address` on VLAN 30, prerequisite 3 = uplink `physical` network `parent=fast40` + `ipv4.ovn.ranges` (trunk work done), Geneve on VLAN 30, spike step reworded. ARCHITECTURE_GO — `features.networks` requires OVN (verified in Incus project reference), so slice-1 default bridge = `ac-<sandbox>-default` in the Incus `default` project with `user.agentcompute.sandbox` metadata and `restricted.networks.access`; sandbox projects flip to `features.networks=true` with OVN; bridge sandboxes drained not converted.
Planner findings left for Josh (PLAN.md §2): TTL promise vs stdio-process reaper; deletion ordering with published images/forwards; screenshot URL reachability needs one real agent; Windows/macOS legal gates; VLAN 40 pool size (.200–.250).

## 2026-09-11 13:07 — Open questions consolidated with recommendations
Fifteen items grouped by phase; my recommendations (pending Josh): keep `sandbox`; TTL is best-effort until the Phase 9 cluster deployment (scope the wording, no second process); blocking ops stay; `desktop.call` pass-through first; stay on VLAN 40 through the OVN spike and decide at Phase 5 on the size of `ipv4.ovn.ranges` (a /26 reservation fits → keep VLAN 40); OVN central = one OpenTofu VM on nas01 VLAN 10, no raft; reaper deletes in dependency order after a Phase 2 residue spike; screenshots by URL only, verified with a real agent in Phase 6; Driver token continuity is the Phase 6 spike (fallback: long-lived `cua-driver serve` per instance over exec); pin newest non-prerelease Driver; dedicated Apple Silicon box (Mac mini) as Lume host + self-hosted runner; do NOT publish Windows/macOS images to GHCR — keep them lab-local (Incus alias / Lume seed) and reserve imgoci for Linux; read session 018 notes at close; promote T48/fast40 corrections into TECH_NOTES at close.

## 2026-09-11 13:14 — Josh's rulings applied to the draft
Josh: agree with all recommendations; deployment must be HTTP, stdio is development-only. Draft `7482db4`: "Where the server runs" rewritten (HTTP service in a cluster VM is the deployment and the only form under which the TTL guarantee holds; stdio dev-only, exits with client); Reliability section states reaper lives in the deployed service and deletes in dependency order; Windows/macOS catalog rows marked cluster-local / Mac-local, never published; Linux via imgoci/GHCR; macOS host prerequisite = dedicated Apple Silicon box (Mac mini); Open Questions restructured into "Resolved in review" (sandbox name, URL-only screenshots, Mac, HTTP deployment, blocking, single OVN central, no Windows/macOS publication, desktop.call pass-through) and "Still open" (VLAN 40 vs new — decided at durable uplink on `ipv4.ovn.ranges` size; Driver token continuity — desktop spike; Driver-in-image recipe). ARCHITECTURE_GO aligned (HTTP deployment note; reaper dependency-order deletion incl. default-project bridges).

## 2026-09-11 13:25 — Phase prompts written
Nine per-phase prompts in `.journal/019/prompts/` (`00-README.md` index + `01`–`09`, ~9k words), one file per PLAN.md phase, each self-contained: documents to read (absolute paths), the spike that comes first with its decision rule, deliverables per repository with the worktree+PR workflow, working rules (designs fixed; findings go in the report), and acceptance evidence. Research reports persisted verbatim to `.journal/019/research/{linux,windows,macos}-images.md` so prompts don't depend on `agent://` handles.
Repo decision from Josh recorded in PLAN.md header and Phase 1 prompt: new `GilmanLab/agentcompute` generated from `meigma/template-mcp-codemode` (repo does not exist yet; `gh repo view` 404), cloned by `init.sh` into `~/code/lab2/agentcompute` as an ignored sub-repo; Phase 1 adds it to `init.sh`, `.gitignore`, and `AGENTS.md` in a root PR. Prompts assume codemode ≥ v0.2.1 and incus-gh-runner v2.0.0.

## 2026-09-11 14:54 — Phase 1 done; findings applied
`GilmanLab/agentcompute` exists (PUBLIC), cloned at `~/code/lab2/agentcompute`; root #29 registered it in `init.sh`. Phase 1 shipped `images/{pins.yaml,catalog.yaml,router/,build.py}`, publish pipeline, `spikes/images` reconciler; `router` published as imgoci release `ghcr.io/gilmanlab/agentcompute/router@sha256:6b3ecd83…` (Alpine 3.22.5; every apk pinned by URL+SHA), imported and boot-tested; Incus fingerprint recorded as derived.
Agent findings → applied: (1) public repo ⇒ Phase 4 needs the incus-gh-runner threat review before a self-hosted scale set — added as a hard gate in `prompts/04` with the concrete checks (no `pull_request` triggers on the publisher workflow, `IMAGES_RUNNER` repo variable, fork approval settings, repo-scoped scale set); (2) catalog `reference` is `ref@digest` for lab-built and an Incus alias for upstream — ARCHITECTURE_GO config now points at `images/catalog.yaml` (schema_version 1) and the reconciler accepts both forms; (3) fingerprints are not reproducible (timestamps) — reconciler keys on the imgoci digest recorded in image properties; fingerprint derived; "same digest as local" dropped as an acceptance test; (4) `features.networks` requires OVN — confirmed again, `image-build` uses the default-project network shape; (5) master run on unchanged tree fails at publish — Phase 4 prompt adds "skip when tag exists"; (6) restricted certs filter reads rather than 403 — ARCHITECTURE_GO testing note + Phase 2/4 prompts assert empty list / refused mutation.

## 2026-09-11 15:06 — Phase 2 stopped at the network spike; resolved
Agent stopped correctly (draft PR agentcompute#14, branch `feat/go-slice1`, `spikes/project-bridge/probe.py`, `spikes/agent-error/main.go`, `spikes/results.json`). Two Incus facts contradicted ARCHITECTURE_GO: (1) a cluster managed bridge defined with `--target lab01` is only pending; activation requires definition on every member ("Network not defined on nodes: nas01, lab02, lab03") — member-only bridges do not exist; (2) bridge names are host interface names, max 15 chars — `ac-abcde-default` rejected. `AgentError` spike passed end to end on codemode v0.2.1 (`capability failed: instance "x" not found in sandbox "y"`).
Resolution (mechanism, not vocabulary; applied without bouncing to Josh): bridges defined on all members then activated (each member's copy is its own L2 domain = the draft's `kind="bridge"` semantics), bridge-backed sandboxes keep instances on one member and `host?` must match; Incus bridge names are opaque `ac`+8-hex IDs with `user.agentcompute.{sandbox,name,version}` config, agent-facing names mapped by metadata only; `restricted.networks.access` lists Incus names; reaper finds by metadata. ARCHITECTURE_GO "Default network, slice 1" rewritten; draft backend-mapping cell corrected (`5935533`); resume prompt `prompts/02b-go-slice-1-resume.md` written (re-run probe under the new mapping incl. NAT egress, cross-project isolation, cross-member negative check; then build slice 1 on the same branch).
Note for Phase 5: OVN networks inside a project create no host interface, so plain agent-facing names should be legal there — verify.

## 2026-09-11 16:29 — Phase 2 merged (agentcompute#14, `358c572`)
Slice 1 landed: `cmd/agentcompute`, `internal/{cli,compute,incus,mcpserver}`, mockery mocks, contract/handler/integration tests, `spikes/acceptance.star`; second spike run passed on all four members (`spikes/results.json`). Deviations accepted into ARCHITECTURE_GO: `compute.Backend` is a consumer-defined 13-method interface in `compute` (import-cycle reason; slice-scoped, not a hypervisor abstraction — Lume becomes "implement it"); `BeginCreateInstance` → `PendingInstance` so the gate releases after Incus accepts the create; Incus client cloned per request before `WithContext`/`UseProject`/`UseTarget`; images copied into each sandbox project before first create (`features.images=true` hides `default` aliases; `InstanceSource.Project` alone does not copy). Handoffs appended to prompts 03 (prove OVN independently), 05 (drain bridge sandboxes; publish/delete must handle the per-project image copy; plain OVN names likely legal), 06 (screenshots via a binary `ReadFile`, never capped exec), 08 (Lume = implement `compute.Backend`).

## 2026-09-11 19:10 — Phase 3 paused; decoded and resumed
Agent stopped for two reasons. (1) Uplink: my prompt said create a `physical` network on `fast40`, but Phases 1–2 already created managed `fast40-macvlan` (image-build profile) and Incus allows one managed network per parent ("Parent interface \"fast40\" in use by another network"). Incus docs confirm OVN uplinks may be `physical`, `bridge`, or `macvlan` managed networks (standalone example sets `ipv4.ovn.ranges` on an existing parent) → the uplink IS `fast40-macvlan` with `ipv4.ovn.ranges=10.10.40.64-.79`, `ipv4.gateway=10.10.40.1/24`. Nothing OVN was measured yet. (2) Cleanup quirk: `network.ovn.northbound_connection` cannot be unset on IncusOS — the default `unix:/run/ovn/ovnnb_db.sock` is validated by connecting and doesn't exist on chassis-only nodes; disabling chassis with the key set hangs cluster notifications. Only matters for baseline restoration, which we don't want. Rule for Phase 5: chassis stay enabled while the NB key is set; repoint before any chassis change. lab03 reboot with central unreachable returned ONLINE in ~116 s; `initrd-cleanup-root.service` failure to be compared against a non-OVN member. Josh's read ("recommendation = no OVN") was a misparse of a scrupulous cleanup report; wrote `prompts/03b-ovn-spike-resume.md`: pause option, keep central/chassis/NB as-is, uplink on the macvlan, resume from step 5, file the unset quirk upstream.

## 2026-09-11 19:29 — Correction: macvlan cannot be an OVN uplink
My `03b` instruction was wrong: the Incus macvlan network reference has no `ipv4.ovn.ranges`/`ipv4.gateway` keys, so `fast40-macvlan` cannot be the uplink; only `physical`/`bridge` can. The agent correctly fell back to a `physical` `fast40-uplink` and flagged that image-build guests can't remain on `fast40` (NIC on a physical network = passthrough; OVN provider bridge on the parent conflicts with macvlan devices). It offered "move image-build onto OVN" — rejected: CI bakes must not depend on OVN central (currently temporary on sandbox01), and the build path needs only egress + API/exec. Answer given: give `image-build` its own NAT'd managed bridge on all members (the Phase 2-proven shape), repoint the profile via fleet, verify egress + boot-test, then delete `fast40-macvlan` and create `fast40-uplink` (physical). Consequence accepted: `fast40` belongs to OVN from here on; raw VLAN 40 presence for instances goes via OVN forwards or sandbox01.

## 2026-09-11 20:20 — Phase 3 result: NOT SMOOTH, but datapath proven
Agent delivered fleet#14 (chassis, NB, `fast40-uplink` physical, `image-build` NAT bridge `10.57.139.1/24` on all members, `fast40-macvlan` deleted), agentcompute#15 (report/probe), root#30 (reserve `10.10.40.64–.79`). First pass PASSED everything: cross-member ping, HTTPS egress with SNAT `.64`, Tailscale-reachable forward, 100 MB hash-checked transfers both ways, MTU 1442. Address cost: 2 external IPv4 per sandbox (router + one forward). Central-down: existing datapath kept working.
Failures: step 6 — create with central stopped hangs 60 s and leaves an `Errored` network holding `.65` (Incus quirk; recoverable by delete; reaper must handle). Step 7 — after teardown+recreate, east-west fine but gateway/DNS/HTTPS dead: router reused `.64` with a new MAC, gw01 neighbor FAILED, gateway capture saw NO ARP/ICMP from `.64` → the gateway chassis (lab01) never put the router port on `fast40`. Hypotheses: H1 uplink provider-bridge mapping torn down with last consumer and rebuilt incompletely; H2 step-6 `Errored` residue contaminated chassis (steps ran 6→7, confounded); H3 stale ARP (unlikely). Verdict: not evidence against OVN; wrote `prompts/03c-ovn-repeat-diagnosis.md` — three clean cycles, then keeper-network mitigation, then step-6 reproduction; PROCEED/FALLBACK rule. Recommended merging all three PRs now (cluster already converged to that state).

## 2026-09-12 08:23 — 03c interim: lab01 chassis looks broken, not OVN
Agent reports: step-6 reproduction confirmed recoverable (Errored network holding `.66` deleted after central recovery; fresh cycle then passed on nas01); a `soak01` network is live; the keeper network failed qualification and lab01's OVS is stuck retrying a "conflicting" `fast40` attachment (2,085 log messages/s rate-limited). Pattern: cycles pass with nas01 as gateway chassis, fail on lab01 → H2 (chassis state corrupted during the central-down create tests) localized to lab01. Answer given (Other): remove the failed keeper via fleet, keep soak01/uplink/chassis/central; capture lab01's exact OVS conflict; run a lab01-gateway cycle to confirm it alone fails; reboot lab01 (previously authorized pattern) and rerun; pass ⇒ PROCEED with rule "OVN control-plane ops require central up" + reaper handles Errored; fail after reboot ⇒ real bug, FALLBACK with OVS evidence.

## 2026-09-12 10:03 — 03c verdict FALLBACK is premature: root cause is a macvlan fixture on fast40
Agent reported FALLBACK: lab01 as gateway fails before and after reboot; OVS logs `failed to add fast40 as port: Device or resource busy`; noted `soak01` "has an active macvlan NIC there" but did not remove it. Verified read-only: `default/soak01` on lab01 has `eth1 nictype=macvlan parent=fast40` — the only such device in the cluster. Mechanism: macvlan claims the parent's rx_handler; OVS cannot add the parent as a provider-bridge port → that member cannot be an OVN gateway chassis. Reboot cannot fix it (instance returns). Explains lab01-only failures incl. the original step 7. Answer given (Other): delete soak01, confirm OVS attaches fast40 on lab01, rerun lab01-gateway cycle; pass ⇒ PROCEED with rules (nothing but `fast40-uplink` attaches to `fast40`; sandbox projects `restricted.devices.nic=managed`; fleet check on default project; control-plane ops need central up); update lxc/incus#3986 (gateway selection onto a chassis whose uplink parent is claimed). Deliverables so far: agentcompute#16 (13-run cycle table), fleet#16 (keeper deploy, absent), root#31 (address accounting: 2 external IPv4 per sandbox; 16-address block ⇒ 8 sandboxes), upstream lxc/incus#3985 (NB-outage create hangs, Errored network), #3986.
Housekeeping surfaced: lab01's reboot activated IncusOS `202609100026` (kernel 7.2.4-zabbly+) while nas01/lab02/lab03 run `202608242359` — cluster is meant to be release-pinned together; fleet seeds need re-pin + coordinated reboots. TECH_NOTES update at close.
